<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/loginFragment">

    <fragment
        android:id="@+id/addFragment"
        android:name="com.esdut.ciremember.AddFragment"
        android:label="添加单词"
        tools:layout="@layout/fragment_add" />
    <fragment
        android:id="@+id/wordsFragment"
        android:name="com.esdut.ciremember.WordsFragment"
        android:label="词 记"
        tools:layout="@layout/fragment_words">
        <action
            android:id="@+id/action_wordsFragment_to_addFragment"
            app:destination="@id/addFragment"
            app:enterAnim="@anim/nav_default_enter_anim" />
    </fragment>
    <fragment
        android:id="@+id/loginFragment"
        android:name="com.esdut.ciremember.LoginFragment"
        android:label="账户登录"
        tools:layout="@layout/fragment_login">
        <action
            android:id="@+id/action_loginFragment_to_wordsFragment"
            app:destination="@id/wordsFragment" />
    </fragment>
</navigation>